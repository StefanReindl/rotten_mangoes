<h1>Rotten Mangoes</h1>
<%= link_to "Submit a movie!", new_movie_path %>
<hr>

<%= form_tag movies_path, method: :get do %>
	Title: <%= text_field_tag :title, params[:title] %>

	Director: <%= text_field_tag :director, params[:director] %>

	Duration: <%= select_tag :duration, 
		options_for_select({
			'Any' => '0',
			'Under 90 minutes' => '1',
			'Between 90 and 120 minutes' => '2',
			'Over 120 minutes' => '3'
			}, params[:duration]) %>
	
	<%= submit_tag 'Search' %>
<% end %>

<% @movies.each do |movie| %>
  <%= image_tag (movie.image_url(:thumb)).to_s %> 
  <h2><%= link_to movie.title, movie_path(movie) %></h2>
  <%= formatted_date(movie.release_date) %>
  <%= movie.review_average %>/10
  <h4>Dir. <%= movie.director %> | <%= movie.runtime_in_minutes %> minutes</h4>
  <p><%= movie.description %></p>
  <hr>
<% end %>